//complex objects 

const personarray = ["harkirat","sam","jai","pooja"];
const genderarary = ["male","male","male","female"];
const numberOfUsers = personarray.length;
for (let i=0;i<numberOfUsers;i++)
{
    if (genderarary[i]=="male")
    {
        console.log(personarray[i]);
    }
}

// objects is used to combine values in javascript
